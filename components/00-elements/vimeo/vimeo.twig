{# Web component that lazy loads in the Vimeo Iframe #}
{# See kinetic.info.yml. Disabled the video_embed_field/responsive-video library becuase it messes up the CSS  #}
<lite-vimeo videoid="{{ video_id }}" autoplay="{{ autoplay == true? 'true' : 'false' }}">
</lite-vimeo>
{# 27246366 #}