{% set content %}
  {{ include('kinetic:section-region', {region: first, layout__region: 'first'}, with_context = false) }}
  {{ include('kinetic:section-region', {region: second, layout__region: 'second'}, with_context = false) }}
{% endset %}

{{ include('kinetic:section', {
  settings: settings,
  content: content,
  layout_class: 'layout-twocol',
  wrapper_classes: [],
  inner_wrapper_classes: ['container'],
}, with_context: false) }}