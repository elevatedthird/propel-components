{% set content %}
  {{ include('kinetic:section-region', {region: first, layout__region: 'first'}, with_context: true) }}
  {{ include('kinetic:section-region', {region: second, layout__region: 'second'}, with_context: true) }}
{% endset %}

{{ include('kinetic:section', {
  settings: settings,
  content: content,
  layout_class: 'layout-twocol',
  wrapper_classes: [],
  inner_wrapper_classes: ['grid', 'grid-cols-2'],
}, with_context: true) }}