{% set content %}
  {{ include('kinetic:section-region', {region: first, layout__region: 'first'}, with_context = false) }}
{% endset %}

{{ include('kinetic:section', {
  settings: settings,
  content: content,
  layout_class: 'content-header',
  wrapper_classes: [],
  inner_wrapper_classes: [],
}, with_context: false) }}
