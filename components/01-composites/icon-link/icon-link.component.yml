"$schema": "https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json"
name: "Icon Link"
status: "stable"
description: "Displays an icon with a link."
slots:
  icon:
    title: "Icon"
    description: "The icon to display."
props:
  type: object
  properties:
    url:
      type: object
      description: "The URL Object to link to."
    title:
      type: string
      description: "The title attribute for the link."
