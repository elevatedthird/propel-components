<article class="flex flex-col md:flex-row gap-4 relative bg-gray-100 rounded-lg p-4">
  <div aria-hidden="true" class="md:max-h-[134px] aspect-3/2 rounded-lg overflow-hidden [&_img]:cover">{{ image }}</div>
  <h4 class="text-purple text-[15px] font-semibold">
    <a href="{{ url }}" class="after:absolute after:inset-0"> {{ title }} </a>
  </h4>
</article>