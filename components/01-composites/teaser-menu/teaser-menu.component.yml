"$schema": "https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json"
name: "Teaser - Menu"
status: "stable"
description: "A small teaser that displays in the menu."
slots:
  image:
    title: Image
    description: The media slot.
props:
  type: object
  properties:
    heading:
      type: string
      title: Card heading
      required: true
    url:
      type: string
      title: URL
      description: The link to the card.
