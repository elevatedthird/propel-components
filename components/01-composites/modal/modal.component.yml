$schema: https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json
name: Modal
status: stable
description: A micromodal modal with a trigger and a dialog.
needs:
  - dialog
required:
  - modal_trigger
  - modal_dialog
  - modal_heading
slots:
  modal_dialog:
    title: Modal Dialog
    description: The dialog of the modal.
  modal_heading:
    title: Modal Heading
    description: The heading of the modal.
props:
  type: object
  properties:
    modal_trigger:
      title: Modal Trigger
      type: string
      description: The trigger for the modal.
